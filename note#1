SQL allows for four types of commands:
  Create, Insert
  Read - Select
  Update 
  Delete, Drop


SQL supports many commands to access data, including:
  AVG
  COUNT
  DISTINCT
  LOWER
  MAX
  MIN
  UPPER 